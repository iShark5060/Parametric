# ──────────────────────────────────────────────
# Parametric - Environment Configuration
# ──────────────────────────────────────────────
# Copy this file to .env and fill in values.
# .env is gitignored and never committed.
# ──────────────────────────────────────────────

# Server
NODE_ENV=production
PORT=3001

# Session secret — generate a strong random string (32+ characters)
SESSION_SECRET=<generate-a-strong-secret>

# Shared central database for users, sessions, and game access.
# Both Parametric and Corpus must point to the SAME file for shared auth.
# Use an absolute path in production (e.g., /opt/data/central.db).
# CENTRAL_DB_PATH=data/central.db

# Reverse proxy settings (set to 1 when behind nginx/caddy)
TRUST_PROXY=1
SECURE_COOKIES=1

# Cookie domain for session sharing across subdomains (e.g., .shark5060.net).
# Omit on localhost.
# COOKIE_DOMAIN=.shark5060.net

# Base host for this app (used for redirect URLs)
# BASE_HOST=parametric.shark5060.net

# Central Auth service (new unified auth app)
AUTH_SERVICE_URL=https://auth.shark5060.net

# Auth rate-limiting / lockout
AUTH_MAX_ATTEMPTS=5
AUTH_LOCKOUT_MINUTES=15
AUTH_ATTEMPT_WINDOW_MINUTES=15