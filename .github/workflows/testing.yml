name: Run Tests

permissions:
  contents: read

on:
  pull_request:

jobs:
  test:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    env:
      DOTENV_PRIVATE_KEY_DEVELOPMENT: ${{ secrets.DOTENV_PRIVATE_KEY_DEVELOPMENT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '25'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate dotenv private key
        run: |
          if [ -z "$DOTENV_PRIVATE_KEY_DEVELOPMENT" ]; then
            echo "ERROR: DOTENV_PRIVATE_KEY_DEVELOPMENT secret is missing or empty."
            exit 1
          fi

      - name: Build project
        run: npx dotenvx run --strict -f .env.development -- npm run build

      - name: Run tests
        run: npx dotenvx run --strict -f .env.development -- npm run test
